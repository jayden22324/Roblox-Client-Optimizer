[package]
name = "rco3launcher"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11.18", features = ["blocking", "json"] }

[[bin]]
name = "rco3launcher"
path = "src/main.rs"
